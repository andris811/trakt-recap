{
  "name": "trakt-recap",
  "version": "1.0.0",
  "description": "Personal Trakt analytics dashboard",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "express": "^4.21.0",
    "dotenv": "^16.4.0",
    "@supabase/supabase-js": "^2.39.0"
  }
}
